---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: kesrotations.cardano.cardano.org
spec:
  group: cardano.cardano.org
  names:
    categories:
    - cardano
    kind: KESRotation
    listKind: KESRotationList
    plural: kesrotations
    shortNames:
    - kesr
    singular: kesrotation
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.stakePoolRef
      name: StakePool
      type: string
    - jsonPath: .status.currentKESPeriod
      name: KES Period
      type: integer
    - jsonPath: .status.kesExpiryEpoch
      name: Expiry Epoch
      type: integer
    - jsonPath: .status.nextRotationEpoch
      name: Next Rotation
      type: integer
    - jsonPath: .spec.autoRotate
      name: Auto
      type: boolean
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: KESRotation is the Schema for the kesrotations API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: KESRotationSpec defines the desired state of KESRotation
            properties:
              autoRotate:
                default: true
                description: 'Enable automatic rotation (default: true)'
                type: boolean
              maxKESEvolutions:
                default: 62
                description: Max KES evolutions (protocol param, usually 62)
                format: int32
                type: integer
              rotationLeadEpochs:
                default: 2
                description: 'Epochs before expiry to trigger rotation (default: 2)'
                format: int32
                maximum: 10
                minimum: 1
                type: integer
              stakePoolRef:
                description: Associated StakePool name
                type: string
            required:
            - stakePoolRef
            type: object
          status:
            description: KESRotationStatus defines the observed state of KESRotation
            properties:
              conditions:
                description: Standard conditions
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              currentKESPeriod:
                description: Current KES period number
                format: int64
                type: integer
              kesExpiryEpoch:
                description: Epoch when current KES expires
                format: int64
                type: integer
              kesStartSlot:
                description: Slot when current KES became active
                format: int64
                type: integer
              nextRotationEpoch:
                description: Planned rotation epoch
                format: int64
                type: integer
              rotationHistory:
                description: Recent rotation events (last 10)
                items:
                  description: RotationEvent records a KES rotation event
                  properties:
                    certificateTxHash:
                      description: Certificate update transaction hash
                      type: string
                    errorMessage:
                      description: Error if failed
                      type: string
                    expiryEpoch:
                      description: Expiry epoch of new KES
                      format: int64
                      type: integer
                    kesPeriod:
                      description: KES period that was rotated
                      format: int64
                      type: integer
                    rotatedAt:
                      description: Rotation timestamp
                      format: date-time
                      type: string
                    success:
                      description: Whether rotation succeeded
                      type: boolean
                  required:
                  - expiryEpoch
                  - kesPeriod
                  - rotatedAt
                  - success
                  type: object
                type: array
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
