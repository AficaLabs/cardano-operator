---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: cardanonodes.cardano.cardano.org
spec:
  group: cardano.cardano.org
  names:
    categories:
    - cardano
    kind: CardanoNode
    listKind: CardanoNodeList
    plural: cardanonodes
    shortNames:
    - cn
    singular: cardanonode
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.type
      name: Type
      type: string
    - jsonPath: .spec.network
      name: Network
      type: string
    - jsonPath: .status.phase
      name: Phase
      type: string
    - jsonPath: .status.syncProgress
      name: Sync
      type: string
    - jsonPath: .status.peerCount
      name: Peers
      type: integer
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: CardanoNode is the Schema for the cardanonodes API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: CardanoNodeSpec defines the desired state of CardanoNode
            properties:
              network:
                description: 'Network: mainnet, preprod, preview'
                enum:
                - mainnet
                - preprod
                - preview
                type: string
              nodeVersion:
                description: Cardano node version
                type: string
              resources:
                description: CPU/memory configuration
                properties:
                  claims:
                    description: |-
                      Claims lists the names of resources, defined in spec.resourceClaims,
                      that are used by this container.

                      This is an alpha field and requires enabling the
                      DynamicResourceAllocation feature gate.

                      This field is immutable. It can only be set for containers.
                    items:
                      description: ResourceClaim references one entry in PodSpec.ResourceClaims.
                      properties:
                        name:
                          description: |-
                            Name must match the name of one entry in pod.spec.resourceClaims of
                            the Pod where this field is used. It makes that resource available
                            inside a container.
                          type: string
                        request:
                          description: |-
                            Request is the name chosen for a request in the referenced claim.
                            If empty, everything from the claim is made available, otherwise
                            only the result of this request.
                          type: string
                      required:
                      - name
                      type: object
                    type: array
                    x-kubernetes-list-map-keys:
                    - name
                    x-kubernetes-list-type: map
                  limits:
                    additionalProperties:
                      anyOf:
                      - type: integer
                      - type: string
                      pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                      x-kubernetes-int-or-string: true
                    description: |-
                      Limits describes the maximum amount of compute resources allowed.
                      More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
                    type: object
                  requests:
                    additionalProperties:
                      anyOf:
                      - type: integer
                      - type: string
                      pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                      x-kubernetes-int-or-string: true
                    description: |-
                      Requests describes the minimum amount of compute resources required.
                      If Requests is omitted for a container, it defaults to Limits if that is explicitly specified,
                      otherwise to an implementation-defined value. Requests cannot exceed Limits.
                      More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
                    type: object
                type: object
              stakePoolRef:
                description: Owning StakePool name (for BP nodes)
                type: string
              storage:
                description: PVC configuration
                properties:
                  accessModes:
                    description: 'Access modes (default: ReadWriteOnce)'
                    items:
                      type: string
                    type: array
                  size:
                    description: PVC size (e.g., "200Gi")
                    pattern: ^[0-9]+[KMGTPE]i?$
                    type: string
                  storageClassName:
                    description: 'StorageClass name (default: cluster default)'
                    type: string
                required:
                - size
                type: object
              topology:
                description: Peer topology configuration
                properties:
                  mode:
                    description: 'Topology mode: static or p2p'
                    enum:
                    - static
                    - p2p
                    type: string
                  p2pConfig:
                    description: P2P configuration (if mode=p2p)
                    properties:
                      targetNumberOfActivePeers:
                        default: 20
                        description: Target number of active peers
                        format: int32
                        type: integer
                      targetNumberOfEstablishedPeers:
                        default: 40
                        description: Target number of established peers
                        format: int32
                        type: integer
                    type: object
                  staticPeers:
                    description: Static peer list (if mode=static)
                    items:
                      description: Peer defines a static peer configuration
                      properties:
                        address:
                          description: IP or hostname
                          type: string
                        port:
                          description: Port number
                          format: int32
                          maximum: 65535
                          minimum: 1
                          type: integer
                      required:
                      - address
                      - port
                      type: object
                    type: array
                required:
                - mode
                type: object
              type:
                description: 'Node type: block-producer, relay, or offline-signing'
                enum:
                - block-producer
                - relay
                - offline-signing
                type: string
            required:
            - network
            - storage
            - type
            type: object
          status:
            description: CardanoNodeStatus defines the observed state of CardanoNode
            properties:
              conditions:
                description: Standard conditions
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              nodeVersion:
                description: Running node version
                type: string
              peerCount:
                description: Connected peer count
                format: int32
                type: integer
              phase:
                description: Current phase
                enum:
                - Pending
                - Starting
                - Syncing
                - Running
                - Error
                type: string
              podName:
                description: Associated Pod name
                type: string
              serviceName:
                description: Associated Service name
                type: string
              syncProgress:
                description: Sync percentage (e.g., "99.5%")
                type: string
              tipEpoch:
                description: Current epoch
                format: int64
                type: integer
              tipSlot:
                description: Current chain tip slot
                format: int64
                type: integer
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
