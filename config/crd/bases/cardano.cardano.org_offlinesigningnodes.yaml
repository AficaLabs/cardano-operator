---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: offlinesigningnodes.cardano.cardano.org
spec:
  group: cardano.cardano.org
  names:
    categories:
    - cardano
    kind: OfflineSigningNode
    listKind: OfflineSigningNodeList
    plural: offlinesigningnodes
    shortNames:
    - osn
    singular: offlinesigningnode
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.stakePoolRef
      name: StakePool
      type: string
    - jsonPath: .spec.signingMode
      name: Mode
      type: string
    - jsonPath: .status.phase
      name: Phase
      type: string
    - jsonPath: .status.pendingRequests
      name: Pending
      type: integer
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: OfflineSigningNode is the Schema for the offlinesigningnodes
          API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: OfflineSigningNodeSpec defines the desired state of OfflineSigningNode
            properties:
              requestRetentionDays:
                default: 30
                description: 'Days to retain completed requests (default: 30)'
                format: int32
                type: integer
              signingMode:
                description: 'Signing mode: manual (user exports/imports) or isolated
                  (dedicated namespace)'
                enum:
                - manual
                - isolated
                type: string
              stakePoolRef:
                description: Associated StakePool name
                type: string
            required:
            - signingMode
            - stakePoolRef
            type: object
          status:
            description: OfflineSigningNodeStatus defines the observed state of OfflineSigningNode
            properties:
              completedRequests:
                description: Recently completed requests
                items:
                  description: SigningRequest represents a transaction signing request
                  properties:
                    createdAt:
                      description: Request creation time
                      format: date-time
                      type: string
                    errorMessage:
                      description: Error details if failed
                      type: string
                    id:
                      description: Unique request ID
                      type: string
                    signedTx:
                      description: Base64-encoded signed transaction (user provides)
                      type: string
                    status:
                      description: Current status
                      enum:
                      - pending
                      - awaiting-signature
                      - signed
                      - submitted
                      - confirmed
                      - failed
                      type: string
                    txHash:
                      description: Submitted transaction hash
                      type: string
                    type:
                      description: Type of signing request
                      enum:
                      - registration
                      - update
                      - kes-rotation
                      - retirement
                      type: string
                    unsignedTxBody:
                      description: Base64-encoded unsigned transaction body
                      type: string
                    unsignedTxHash:
                      description: Hash for verification
                      type: string
                  required:
                  - createdAt
                  - id
                  - status
                  - type
                  type: object
                type: array
              conditions:
                description: Standard conditions
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              lastActivity:
                description: Last signing activity timestamp
                format: date-time
                type: string
              pendingRequests:
                description: Queue of pending signing requests
                items:
                  description: SigningRequest represents a transaction signing request
                  properties:
                    createdAt:
                      description: Request creation time
                      format: date-time
                      type: string
                    errorMessage:
                      description: Error details if failed
                      type: string
                    id:
                      description: Unique request ID
                      type: string
                    signedTx:
                      description: Base64-encoded signed transaction (user provides)
                      type: string
                    status:
                      description: Current status
                      enum:
                      - pending
                      - awaiting-signature
                      - signed
                      - submitted
                      - confirmed
                      - failed
                      type: string
                    txHash:
                      description: Submitted transaction hash
                      type: string
                    type:
                      description: Type of signing request
                      enum:
                      - registration
                      - update
                      - kes-rotation
                      - retirement
                      type: string
                    unsignedTxBody:
                      description: Base64-encoded unsigned transaction body
                      type: string
                    unsignedTxHash:
                      description: Hash for verification
                      type: string
                  required:
                  - createdAt
                  - id
                  - status
                  - type
                  type: object
                type: array
              phase:
                description: Current phase
                enum:
                - Idle
                - RequestPending
                - AwaitingSignature
                - Processing
                - Error
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
